# Docker environment configuration
# Copy this file to .env and adjust values as needed

# Environment type: development or production
ENVIRONMENT=development

# User/Group IDs for container permissions (typically your user ID)
UID=1000
GID=1000

# Backend configuration
BACKEND_PORT=8000
USE_CELERY=true

# Frontend development server configuration
VITE_DEV_SERVER_HOST=0.0.0.0
VITE_DEV_SERVER_PORT=5173

# Environment type for config files (development or production)
# This determines which environment-specific config files are loaded

# Restart policy: no, always, unless-stopped, on-failure
RESTART_POLICY=no