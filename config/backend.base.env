# Backend Base Configuration - Shared Settings
# These settings are common across all environments

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Task Queue Configuration
USE_CELERY=1

# File and Data Directory Settings
TEMP_DIR=/app/temp/
APP_DATA_DIR=../data

# Processing Limits - Base defaults (override in environment-specific files)
MAX_CONCURRENT_GENERATIONS=10
MAX_GENERATIONS_PER_HOUR=100
MAX_CONCURRENT_PER_IP=5
RATE_LIMIT_REDIS_PREFIX=rate_limit

# Mesh Generation Settings
# Using HMM + Manifold3D for superior performance (100x faster than OpenSCAD)

# Cleanup Settings - Base defaults
CLEANUP_INTERVAL=900    # 15 minutes
FILE_RETENTION=3600     # 1 hour

# CORS Configuration - Base defaults
CORS_ALLOW_CREDENTIALS=1

# Security Features - Base defaults (override for specific environments)
SECURE_FRAME_DENY=1
SECURE_REFERRER_POLICY=strict-origin-when-cross-origin