# PNPM configuration
# Global store in build directory
store-dir=build/.pnpm-store

# Cache directory
cache-dir=build/.pnpm-cache

# Use symlinks for node_modules
node-linker=hoisted

# Hoist all packages
hoist-pattern=*