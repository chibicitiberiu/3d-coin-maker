# Override for APScheduler mode (no Redis/Celery needed)
services:
  backend:
    environment:
      USE_CELERY: "false"
    # Remove dependency on redis since it's not needed
    depends_on: []

# Don't include redis, celery, or celery-beat services
# They are in profiles in the main compose file